---
import LinkWithArrow from "../LinkWithArrow/LinkWithArrow.astro";

interface Props {
  title: string;
  href: string;
}

const { title, href } = Astro.props;
---

<article
  class="flex h-full flex-col rounded-xl border border-gray-100 bg-white p-6 shadow-sm transition-shadow hover:shadow-md"
>
  <div class="flex items-start justify-between gap-3">
    <span
      class="inline-flex rounded-full bg-tebra-hero-bg px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide text-tebra-teal-dark"
    >
      Press Release
    </span>
    <span class="text-xs font-medium text-gray-400">Tebra</span>
  </div>
  <h3 class="mt-4 flex-1 font-display text-lg font-bold leading-snug text-tebra-teal-dark">
    <a href={href} class="hover:underline focus:outline-none focus-visible:ring-2 focus-visible:ring-tebra-teal">
      {title}
    </a>
  </h3>
  <div class="mt-6">
    <LinkWithArrow label="View all" href={href} external={true} variant="tebra" class="text-sm" />
  </div>
</article>
