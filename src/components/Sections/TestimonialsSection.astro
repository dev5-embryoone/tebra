---
import TestimonialCard from "../Cards/TestimonialCard.astro";
import SectionHeader from "../SectionHeader/SectionHeader.astro";
import { testimonials } from "../../data/testimonials";
---

<section id="resources" class="page-section bg-gray-50">
  <div class="page-container">
    <SectionHeader
      eyebrow="Operational value"
      title="Operational Value Across IVF Workflows"
      description="EmbryoOne helps IVF teams reduce manual work, improve traceability, and standardize processes across cycles"
      centered
    />

    <div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      {testimonials.map((testimonial) => (
        <TestimonialCard
          quote={testimonial.quote}
          author={testimonial.author}
          company={testimonial.company}
          avatarInitials={testimonial.avatarInitials}
        />
      ))}
    </div>
  </div>
</section>

