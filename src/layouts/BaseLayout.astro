---
import "../styles/global.css";
import Navbar from "../components/Navbar/Navbar.astro";
import Footer from "../components/Footer/Footer.astro";
import ProductTourHeader from "../components/ProductTour/ProductTourHeader.astro";
import AutoBreadcrumb from "../components/Common/AutoBreadcrumb.astro";

interface Props {
  title?: string;
  description?: string;
  /** Extra classes on `<main>` (merged with `min-w-0 w-full`). E.g. `main-full-bleed` from global.css. */
  mainClass?: string;
  /** `logo-only` = product-tour style bar (logo only). Default = full site navbar. */
  navMode?: "default" | "logo-only";
  showFooter?: boolean;
  showBreadcrumb?: boolean;
}

const {
  title = "Run your entire practice with one EHR+ platform | Tebra clone",
  description = "Tebra’s EHR+ platform connects care, billing, scheduling, and more with AI-powered tools for private practices.",
  mainClass = "",
  navMode = "default",
  showFooter = true,
  showBreadcrumb = true,
} = Astro.props as Props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Newsreader:opsz,wght@6..72,400;6..72,500;6..72,600;6..72,700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="font-sans bg-white text-gray-900 editorial-compassion">
    <slot name="before-navbar" />
    {navMode === "logo-only" ? <ProductTourHeader /> : <Navbar />}
    <main class:list={["min-w-0 w-full", mainClass]}>
      {showBreadcrumb ? <AutoBreadcrumb /> : null}
      <slot />
    </main>
    {showFooter ? <Footer /> : null}
  </body>
</html>

