# Strapi CMS base URL (no trailing slash). Used when fetching case studies.
# Fallback: if unset or Strapi unreachable, case studies come from src/data/caseStudies.ts
STRAPI_URL=http://localhost:1337
